import CoreGraphics

struct RenderSettings {
    var format: InstagramFormat = .squarePost
    var imageScale: CGFloat = 1.0
    var imageOffset: CGSize = .zero
    var caption: String = ""
    var hashtags: String = "#instagram #content"
}
